# Grafana Dashboards (Optional)

Drop dashboard JSON files in this folder to auto-provision them.

Notes:

- Dashboards in this folder are treated as read-only provisioned dashboards.
- You can still build dashboards in the UI; those live in Grafana's data volume.

