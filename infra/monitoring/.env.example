# Netdata Configuration

# Your server hostname (shown in dashboard)
HOSTNAME=myserver

# Optional: Connect to Netdata Cloud for remote access
# Get token from https://app.netdata.cloud
# NETDATA_CLAIM_TOKEN=
# NETDATA_CLAIM_ROOMS=
