# =============================================================================
# FastAPI Example - Environment Configuration
# =============================================================================
# Copy this file to .env and update with your values
# NEVER commit .env to git!
# =============================================================================

# Application Settings
APP_NAME=my-api
ENVIRONMENT=production
LOG_LEVEL=INFO
IMAGE=ghcr.io/your-org/my-api:1.0.0

# Docker Network (Choose based on environment)
# production: prod-web
# staging: staging-web
# dev: dev-web
DOCKER_NETWORK=prod-web

# CORS Configuration
# In production, specify allowed origins instead of *
# Example: https://app.yourdomain.com,https://www.yourdomain.com
ALLOWED_ORIGINS=*

# Database (if using PostgreSQL)
# DB_USER=apiuser
# DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD
# DB_NAME=api_db
# DB_HOST=my-api-db
# DB_PORT=5432

# Redis (if using cache)
# REDIS_HOST=my-api-redis
# REDIS_PORT=6379
# REDIS_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# External APIs (if needed)
# API_KEY=your-api-key-here
# API_SECRET=your-api-secret-here

# =============================================================================
# Security Notes:
# =============================================================================
# 1. Generate strong passwords: openssl rand -base64 32
# 2. Never commit .env to git
# 3. Use different passwords for dev/staging/production
# 4. Rotate secrets regularly (every 90 days)
# 5. Store production secrets in /var/secrets/ (not in git)
# =============================================================================
