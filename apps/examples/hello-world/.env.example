# =================================================================
# Hello World Example - Environment Variables
# =================================================================

# Environment (dev, staging, or production)
ENVIRONMENT=staging

# Docker network (must match environment)
# dev-web for dev, staging-web for staging, prod-web for production
# IMPORTANT: Also update the network name in compose.yml to match!
DOCKER_NETWORK=staging-web

# Container name will be: app-dev, app-staging, or app-production
